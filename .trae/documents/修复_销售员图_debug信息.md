# 修复计划：销售员图 Debug 信息清理

## 问题分析

根据用户提供的 Debug 信息，打开销售员图时出现大量异常信息：

1. **新厂新系统**：
   - pb_dept_member 表不存在 (ORA-00942)
   - PB_EMPS 表不存在 (ORA-00942)
   - HR_BASE 表获取了 0 条数据

2. **老厂新系统**：
   - pb_dept_member 表获取了 240 条数据 ✓
   - PB_EMPS 表不存在
   - HR_BASE 表获取了 31 条数据

3. **温森新系统**：
   - pb_dept_member 表获取了 217 条数据 ✓
   - PB_EMPS 表不存在
   - HR_BASE 表获取了 40 条数据

## 用户要求

1. 新厂的业务员表使用 **pb_agnt**（而非 pb_dept_member）
2. 不要尝试访问 PB_EMPS 和 HR_BASE 表来补充数据（没必要）

## 当前代码问题

在 `模块_通用函数.cs` 的 `get业务员表()` 方法中：
- 第 161-194 行：尝试查询 pb_dept_member 表
- 第 196-232 行：尝试查询 PB_EMPS 表
- 第 234-273 行：尝试查询 HR_BASE 表

这些查询都会输出 Debug 信息，即使失败也会记录。

## 修复方案

### 方案：简化新系统的业务员数据获取逻辑

对于新系统数据库，只查询该数据库实际存在的表：

1. **新厂新系统** (36.134.7.141)：使用 **pb_agnt** 表
2. **老厂新系统** (36.138.132.30)：使用 **pb_dept_member** 表
3. **温森新系统** (db.05.forestpacking.com)：使用 **pb_dept_member** 表

不再尝试 PB_EMPS 和 HR_BASE 表作为补充数据源。

## 实施步骤

1. **修改 `模块_通用函数.cs`**：
   - 移除 PB_EMPS 表的查询逻辑（第 196-232 行）
   - 移除 HR_BASE 表的查询逻辑（第 234-273 行）
   - 为新厂新系统添加 pb_agnt 表的查询支持
   - **简化 Debug 输出，只保留失败的查询记录**（成功的查询不输出）

2. **需要确认的信息**：
   - pb_agnt 表的字段结构（需要查询确认）

## 预期结果

- 不再出现 PB_EMPS 和 HR_BASE 表的查询失败异常
- 新厂新系统从 pb_agnt 表正确获取业务员数据
- Debug 输出更加简洁，**只显示失败的查询记录**
