# 回滚并修复计划

## 问题现状

我的修改导致了所有厂都报错：**ORA-00942: 表或视图不存在**

原因：
1. 不同厂的数据库表结构不同
2. 新厂、老厂、温森厂的表名和字段名可能不一致
3. 直接使用 `ORD`、`CLNT`、`PRD` 基础表的方式不适用于所有厂

## 回滚方案

### 步骤1：恢复到原来的代码

将 `窗体_利润统计.cs` 中的 `BuildQueryString` 方法恢复到修改前的状态，使用 `V_ORD` 视图。

### 步骤2：仅针对新厂的 ORA-01427 错误进行修复

由于只有新厂出现 ORA-01427 错误，应该：
1. 保持旧系统和温森系统使用原来的 `V_ORD` 视图查询方式
2. 仅对新系统（新厂）采用特殊的处理方式

### 步骤3：正确的修复方式

对于新厂的 ORA-01427 错误，应该：
1. 继续使用 `V_ORD` 视图
2. 在应用程序层面处理重复数据问题
3. 或者使用 `DISTINCT` 或其他方式避免子查询返回多行

## 实施步骤

1. **回滚代码**：恢复 `BuildQueryString` 方法到原始状态
2. **区分处理**：
   - 旧系统：保持原样使用 `V_ORD` 视图
   - 温森系统：保持原样使用 `V_ORD` 视图
   - 新系统：使用修改后的查询方式（仅在确认表结构正确的情况下）
3. **测试验证**：确保三个厂都能正常查询

## 代码修改位置

文件：`h:\TraeDev\易捷业务数据查询\易捷查询CSharp\易捷查询CSharp\窗体_利润统计.cs`
方法：`BuildQueryString`

## 风险说明

- 必须确保恢复到正确的版本
- 需要区分不同系统的数据库结构差异
- 测试时要覆盖所有三个厂
