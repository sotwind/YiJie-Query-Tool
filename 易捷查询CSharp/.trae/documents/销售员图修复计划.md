# 销售员图修复计划

## 问题概述
销售员图查不出数据，且存在debug信息输出。

## 问题分析

### 问题1: Debug信息残留
**位置**: [窗体_销售员图.cs](file:///h:/TraeDev/易捷业务数据查询/易捷查询CSharp/易捷查询CSharp/窗体_销售员图.cs#L76-L81)

**代码**:
```csharp
if (((DataRowView)列表_业务员.Items[i])["EMPNME"].ToString() == "吴玉龙") {
    Debug.WriteLine(((DataRowView)列表_业务员.Items[i])["EMPNME"].ToString());
    Debug.WriteLine(((DataRowView)列表_业务员.Items[i])["EMPCDE"].ToString());
    Debug.WriteLine(((DataRowView)列表_业务员.Items[i])["TEMCDE"].ToString());
    Debug.WriteLine(((DataRowView)列表_业务员.Items[i])["TEMNME"].ToString());
}
```

**修复方案**: 删除这段debug代码

---

### 问题2: 业务员代码字段为空导致SQL条件错误
**位置**: [窗体_销售员图.cs](file:///h:/TraeDev/易捷业务数据查询/易捷查询CSharp/易捷查询CSharp/窗体_销售员图.cs#L83-L87)

**问题描述**: 代码在构建SQL的IN条件时，同时添加了EMPCDE和EMPCDE2，但当EMPCDE2为空字符串时，会生成类似 `'13800138000',''` 的SQL，导致查询异常。

**当前代码**:
```csharp
tmpstr += "'" + ((DataRowView)列表_业务员.Items[i])["EMPCDE"].ToString() + "'";
tmpstr += ",";
tmpstr += "'" + ((DataRowView)列表_业务员.Items[i])["EMPCDE2"].ToString() + "'";
```

**修复方案**: 添加空值检查，只有当EMPCDE2不为空时才添加

---

### 问题3: 部门筛选时的业务员代码字段同样问题
**位置**: [窗体_销售员图.cs](file:///h:/TraeDev/易捷业务数据查询/易捷查询CSharp/易捷查询CSharp/窗体_销售员图.cs#L55-L65)

**当前代码**:
```csharp
if (rv["EMPCDE"].ToString() != "0") {
    if (rv["EMPCDE"] != null && rv["EMPCDE"].ToString() != "") {
        if (tmpstr != "") { tmpstr += ","; }
        tmpstr += "'" + rv["EMPCDE"].ToString() + "'";
    }
    if (rv["EMPCDE2"] != null && rv["EMPCDE2"].ToString() != "") {
        if (tmpstr != "") { tmpstr += ","; }
        tmpstr += "'" + rv["EMPCDE2"].ToString() + "'";
    }
}
```

**修复方案**: 这段代码已经有空值检查，但需要确认逻辑正确性

---

## 修复任务列表

### [x] Task 1: 删除Debug信息
- **优先级**: P1
- **文件**: [窗体_销售员图.cs](file:///h:/TraeDev/易捷业务数据查询/易捷查询CSharp/易捷查询CSharp/窗体_销售员图.cs)
- **行号**: 76-81
- **操作**: 删除整个if语句块
- **状态**: 已完成

### [x] Task 2: 修复业务员选择时的空值问题
- **优先级**: P0
- **文件**: [窗体_销售员图.cs](file:///h:/TraeDev/易捷业务数据查询/易捷查询CSharp/易捷查询CSharp/窗体_销售员图.cs)
- **行号**: 83-87
- **操作**: 添加EMPCDE2的空值检查
- **状态**: 已完成

### [x] Task 3: 验证修复结果
- **优先级**: P1
- **操作**: 编译并测试销售员图功能
- **状态**: 编译成功

---

## 代码修复详情

### Task 1 修复代码
删除第76-81行的if语句块。

### Task 2 修复代码
将第83-87行:
```csharp
if (tmpstr != "") { tmpstr += ","; }
tmpstr += "'" + ((DataRowView)列表_业务员.Items[i])["EMPCDE"].ToString() + "'";
tmpstr += ",";
tmpstr += "'" + ((DataRowView)列表_业务员.Items[i])["EMPCDE2"].ToString() + "'";
```

修改为:
```csharp
if (tmpstr != "") { tmpstr += ","; }
tmpstr += "'" + ((DataRowView)列表_业务员.Items[i])["EMPCDE"].ToString() + "'";
var empCode2 = ((DataRowView)列表_业务员.Items[i])["EMPCDE2"].ToString();
if (!string.IsNullOrEmpty(empCode2)) {
    tmpstr += ",";
    tmpstr += "'" + empCode2 + "'";
}
```
