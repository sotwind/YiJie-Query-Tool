# 易捷查询 - 最终修复方案

## 问题确认

### 历史验证结果
- **老厂新系统**: 2026-03-04 有 201 单，金额 ¥541,413.76
- **温森新系统**: 2026-03-04 有 1,201 单，金额 ¥1,669,488.77
- **数据存在**，但无法正确关联业务员和部门信息

### 根本原因
1. **字段名错误**: `pb_dept_member` 表中没有 `mobile` 字段
2. **JOIN 条件错误**: 使用了错误的关联字段
3. **筛选条件错误**: 引用了不存在的表别名

---

## 修复步骤

### 修复 1: 窗体_利润统计.cs - BuildQueryString() 方法

**原代码 (约第 95-115 行)**:
```csharp
string sql = @"
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    e.empnme as 业务员，  -- 注意：这里用的是 empnme，但下面 JOIN 错了
    d.dptnme as 部门，
    nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 报价总金额，
    nvl(b.accamt, 0) as 卖价总金额，
    nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 利润差额，
    case 
        when nvl(b.quoprc, 0) * nvl(b.accnum, 0) = 0 then 0
        else (nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0)) / (nvl(b.quoprc, 0) * nvl(b.accnum, 0)) * 100
    end as 利率
FROM ord_bas b
LEFT JOIN pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ord_ct t ON b.serial = t.serial
LEFT JOIN pb_dept_member e ON t.agntcde = e.mobile  -- ❌ 错误！没有 mobile 字段
LEFT JOIN pb_dept d ON e.dept_cde = d.dept_cde
WHERE b.isactive = 'Y'
  AND b.created >= to_date('" + 日期_从.Value.Date.ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')" +
            @"  AND b.created < to_date('" + 日期_到.Value.Date.AddDays(1).ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')";
```

**修复后代码**:
```csharp
string sql = @"
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    m.empnme as 业务员，
    m.dptnme as 部门，
    nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 报价总金额，
    nvl(b.accamt, 0) as 卖价总金额，
    nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 利润差额，
    case 
        when nvl(b.quoprc, 0) * nvl(b.accnum, 0) = 0 then 0
        else (nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0)) / (nvl(b.quoprc, 0) * nvl(b.accnum, 0)) * 100
    end as 利率
FROM ord_bas b
LEFT JOIN pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ord_ct t ON b.serial = t.serial
LEFT JOIN pb_dept_member m ON t.agntcde = m.empcde AND m.isactive = 'Y'  -- ✅ 使用 empcde
WHERE b.isactive = 'Y'
  AND b.created >= to_date('" + 日期_从.Value.Date.ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')" +
            @"  AND b.created < to_date('" + 日期_到.Value.Date.AddDays(1).ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')";
```

**关键修改**:
- `e.mobile` → `m.empcde`
- `e.empnme` → `m.empnme`
- `d.dptnme` → `m.dptnme`
- 移除了 `pb_dept` 的 JOIN（因为 `pb_dept_member` 已经有 `dptnme` 字段）
- 添加了 `AND m.isactive = 'Y'` 条件

---

### 修复 2: 窗体_利润统计.cs - 部门筛选条件

**原代码 (约第 118-135 行)**:
```csharp
if (列表_部门.CheckedItems.Count > 0)
{
    string tmpstr = "";
    foreach (DataRowView rowview in 列表_部门.CheckedItems)
    {
        string 部门编码 = rowview["TEMCDE"].ToString();
        if (部门编码 != "" && 部门编码 != null)
        {
            if (tmpstr != "") tmpstr += ",";
            tmpstr += "'" + 部门编码 + "'";
        }
    }

    if (tmpstr != "")
    {
        sql += @" AND d.dept_cde IN (" + tmpstr + @")";  // ❌ d 表已不存在
    }
}
```

**修复后代码**:
```csharp
if (列表_部门.CheckedItems.Count > 0)
{
    string tmpstr = "";
    foreach (DataRowView rowview in 列表_部门.CheckedItems)
    {
        string 部门名称 = rowview["TEMNME"].ToString();  // 使用部门名称而不是编码
        if (部门名称 != "" && 部门名称 != null)
        {
            if (tmpstr != "") tmpstr += ",";
            tmpstr += "'" + 部门名称 + "'";
        }
    }

    if (tmpstr != "")
    {
        sql += @" AND m.dptnme IN (" + tmpstr + @")";  // ✅ 使用 m.dptnme
    }
}
```

**关键修改**:
- `TEMCDE` → `TEMNME`（使用部门名称）
- `d.dept_cde` → `m.dptnme`

---

### 修复 3: 窗体_利润统计.cs - 业务员筛选条件

**原代码 (约第 137-154 行)**:
```csharp
if (列表_业务员.CheckedItems.Count > 0)
{
    string tmpstr = "";
    foreach (DataRowView rowview in 列表_业务员.CheckedItems)
    {
        string 业务员编码 = rowview["EMPCDE"].ToString();
        if (业务员编码 != "" && 业务员编码 != null)
        {
            if (tmpstr != "") tmpstr += ",";
            tmpstr += "'" + 业务员编码 + "'";
        }
    }

    if (tmpstr != "")
    {
        sql += @" AND h.mobile IN (" + tmpstr + @")";  // ❌ h 表不存在！
    }
}
```

**修复后代码**:
```csharp
if (列表_业务员.CheckedItems.Count > 0)
{
    string tmpstr = "";
    foreach (DataRowView rowview in 列表_业务员.CheckedItems)
    {
        string 业务员编码 = rowview["EMPCDE"].ToString();
        if (业务员编码 != "" && 业务员编码 != null)
        {
            if (tmpstr != "") tmpstr += ",";
            tmpstr += "'" + 业务员编码 + "'";
        }
    }

    if (tmpstr != "")
    {
        sql += @" AND t.agntcde IN (" + tmpstr + @")";  // ✅ 直接使用 ord_ct 表字段
    }
}
```

**关键修改**:
- `h.mobile` → `t.agntcde`（直接使用订单表的业务员编码字段）

---

### 修复 4: 模块_通用函数.cs - get 业务员表() 方法

**问题**: 部门编码 `TEMCDE` 可能为空，因为只填充了 `TEMCDE2`

**需要添加的代码** (在合并数据时):
```csharp
// 在 dict.TryGetValue 之后的处理逻辑中添加：
if (temp.TEMCDE == null || temp.TEMCDE == "") {
    temp.TEMCDE = t.TEMCDE ?? t.TEMCDE2;  // 优先使用 TEMCDE，否则用 TEMCDE2
}
```

---

## 验证 SQL 语句

### 测试 1: 基础查询（无筛选）
```sql
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    m.empnme as 业务员，
    m.dptnme as 部门，
    nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 报价总金额，
    nvl(b.accamt, 0) as 卖价总金额
FROM ferp.ord_bas b
LEFT JOIN ferp.pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ferp.ord_ct t ON b.serial = t.serial
LEFT JOIN ferp.pb_dept_member m ON t.agntcde = m.empcde AND m.isactive = 'Y'
WHERE b.isactive = 'Y'
  AND b.created >= DATE '2026-03-04'
  AND b.created < DATE '2026-03-05'
ORDER BY b.created
FETCH FIRST 30 ROWS ONLY;
```

### 测试 2: 部门筛选（销售 1 部）
```sql
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    m.empnme as 业务员，
    m.dptnme as 部门，
    nvl(b.accamt, 0) as 卖价总金额
FROM ferp.ord_bas b
LEFT JOIN ferp.pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ferp.ord_ct t ON b.serial = t.serial
LEFT JOIN ferp.pb_dept_member m ON t.agntcde = m.empcde AND m.isactive = 'Y'
WHERE b.isactive = 'Y'
  AND b.created >= DATE '2026-03-04'
  AND b.created < DATE '2026-03-05'
  AND m.dptnme = '销售 1 部'
ORDER BY b.created
FETCH FIRST 30 ROWS ONLY;
```

### 测试 3: 业务员筛选
```sql
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    m.empnme as 业务员，
    m.dptnme as 部门，
    nvl(b.accamt, 0) as 卖价总金额
FROM ferp.ord_bas b
LEFT JOIN ferp.pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ferp.ord_ct t ON b.serial = t.serial
LEFT JOIN ferp.pb_dept_member m ON t.agntcde = m.empcde AND m.isactive = 'Y'
WHERE b.isactive = 'Y'
  AND b.created >= DATE '2026-03-04'
  AND b.created < DATE '2026-03-05'
  AND t.agntcde IN ('13566469967', '13575872636')  -- 替换为实际业务员编码
ORDER BY b.created
FETCH FIRST 30 ROWS ONLY;
```

---

## 预期结果

### 修复前
- ❌ 查询无数据或业务员/部门显示为空
- ❌ 部门筛选无效
- ❌ 业务员筛选报错或无效

### 修复后
- ✅ 正常显示业务员姓名和部门名称
- ✅ 部门筛选正常工作
- ✅ 业务员筛选正常工作
- ✅ 2026-03-04 数据：老厂 201 单，温森 1,201 单

---

## 下一步行动

1. **修改代码**
   - 修改 `窗体_利润统计.cs` 中的 `BuildQueryString()` 方法
   - 修改 `模块_通用函数.cs` 中的部门编码填充逻辑

2. **编译测试**
   - 编译程序
   - 测试利润统计功能
   - 验证部门筛选
   - 验证业务员筛选

3. **应用到其他窗体**
   - 检查 `窗体_销售员图.cs` 是否有类似问题
   - 检查 `窗体_报价差额统计.cs` 是否有类似问题

---

**修复方案创建时间**: 2026-03-05 17:50  
**修复人**: 大龙虾 🦞
