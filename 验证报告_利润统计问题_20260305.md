# 利润统计查询问题验证报告

**验证日期**: 2026-03-05  
**验证人**: 大龙虾 🦞  
**数据日期**: 2026-03-04（昨天）

---

## 一、问题描述

用户反馈：
1. 利润统计查询**没有数据显示**
2. 之前验证时发现有数据，但现在查询后**没有任何数据**
3. 关键信息：**新旧系统的业务员和部门数据结构不一样**

---

## 二、问题分析

### 2.1 核心问题：SQL JOIN 字段名错误

**当前代码（窗体_利润统计.cs）**:
```csharp
LEFT JOIN pb_dept_member e ON t.agntcde = e.mobile
```

**问题**:
- `pb_dept_member` 表中**没有 `mobile` 字段**
- 根据 `模块_通用函数.cs` 中的查询，新系统 `pb_dept_member` 表的字段是：
  - `user_cde` - 业务员编码
  - `empcde` - 员工编码（可能和 user_cde 相同）
  - `user_nme` / `empnme` - 业务员姓名
  - `dept_cde` / `dptcde` - 部门编码

### 2.2 新旧系统数据结构差异

#### 旧系统（临海等）
| 表名 | 字段 | 说明 |
|------|------|------|
| `pb_emps` | `EMPCDE`, `EMPNME`, `TEMCDE`, `TEMNME` | 业务员表，`objtyp='AG'` |

#### 新系统（集团、老厂、新厂、温森）
| 表名 | 字段 | 说明 |
|------|------|------|
| `pb_dept_member` | `USER_CDE`, `EMPCDE`, `USER_NME`, `EMPNME`, `DEPT_CDE` | 业务员表 |
| `pb_dept` | `DEPT_CDE`, `DPTCDE`, `DEPT_NME`, `DPTNME` | 部门表 |
| `hr_base` | `MOBILE`, `EMPNME`, `DPTCDE` | 人力资源表（备用） |

### 2.3 订单数据分布

根据您的说明：
- **易捷集团数据库 (fgrp)**: 主要用于获取部门和业务员映射关系
- **老厂新系统数据库 (ferp)**: 有部分订单数据
- **温森新系统数据库 (ferp)**: 有订单数据
- **新厂新系统数据库 (ferp)**: 有订单数据
- **各子公司服务器**: 都有各自的订单数据

---

## 三、验证 SQL 脚本

已创建测试脚本：`/home/admin/.openclaw/workspace/YiJie-Query-Tool/验证 SQL_利润统计.sql`

### 关键测试项：

1. **测试 1-4**: 检查 `pb_dept_member` 和 `pb_dept` 表结构
2. **测试 5-6**: 查看 `ord_ct` 表中 `agntcde` 的实际数据
3. **测试 7**: 测试 `t.agntcde = e.user_cde` 关联
4. **测试 8**: 测试 `t.agntcde = e.empcde` 关联
5. **测试 9**: 测试 `t.agntcde = e.mobile` 关联（当前错误代码）
6. **测试 10**: 完整利润统计查询
7. **测试 11**: 按部门汇总统计
8. **测试 14**: 使用 `hr_base` 表的备用方案

---

## 四、修复方案

### 方案 A: 使用 pb_dept_member.user_cde（推荐）

**适用场景**: `ord_ct.agntcde` 存储的是业务员用户编码

```csharp
// 窗体_利润统计.cs - BuildQueryString() 方法
string sql = @"
SELECT 
    TO_CHAR(b.created, 'yyyy-MM-dd') as 日期，
    b.serial as 单号，
    c.clntnme as 客户，
    b.prdnme as 产品，
    e.user_nme as 业务员，
    d.dept_nme as 部门，
    nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 报价总金额，
    nvl(b.accamt, 0) as 卖价总金额，
    nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0) as 利润差额，
    case 
        when nvl(b.quoprc, 0) * nvl(b.accnum, 0) = 0 then 0
        else (nvl(b.accamt, 0) - nvl(b.quoprc, 0) * nvl(b.accnum, 0)) 
             / (nvl(b.quoprc, 0) * nvl(b.accnum, 0)) * 100
    end as 利率
FROM ord_bas b
LEFT JOIN pb_clnt c ON b.clntcde = c.clntcde
LEFT JOIN ord_ct t ON b.serial = t.serial
LEFT JOIN pb_dept_member e ON t.agntcde = e.user_cde
LEFT JOIN pb_dept d ON e.dept_cde = d.dept_cde
WHERE b.isactive = 'Y'
  AND b.created >= to_date('" + 日期_从.Value.Date.ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')" +
    @"  AND b.created < to_date('" + 日期_到.Value.Date.AddDays(1).ToString("yyyy-MM-dd") + "', 'yyyy-MM-dd')";
```

### 方案 B: 使用 pb_dept_member.empcde

**适用场景**: `ord_ct.agntcde` 存储的是员工编码

```csharp
LEFT JOIN pb_dept_member e ON t.agntcde = e.empcde
LEFT JOIN pb_dept d ON e.dept_cde = d.dept_cde
```

### 方案 C: 使用 hr_base.mobile（备用方案）

**适用场景**: `ord_ct.agntcde` 存储的是手机号，且 `hr_base` 表有完整映射

```csharp
LEFT JOIN hr_base h ON t.agntcde = h.mobile
LEFT JOIN pb_dept d ON h.dptcde = d.dept_cde
```

### 方案 D: 组合方案（最保险）

**适用场景**: 不确定哪个字段匹配，使用 COALESCE 尝试多个字段

```csharp
LEFT JOIN pb_dept_member e ON t.agntcde IN (e.user_cde, e.empcde, e.mobile)
LEFT JOIN pb_dept d ON e.dept_cde = d.dept_cde
```

---

## 五、业务员筛选条件修复

**当前代码（错误）**:
```csharp
if (列表_业务员.CheckedItems.Count > 0)
{
    // ...
    sql += @" AND h.mobile IN (" + tmpstr + @")";  // ❌ h 表未定义
}
```

**修复后**:
```csharp
if (列表_业务员.CheckedItems.Count > 0)
{
    string tmpstr = "";
    foreach (DataRowView rowview in 列表_业务员.CheckedItems)
    {
        string 业务员编码 = rowview["EMPCDE"].ToString();
        if (业务员编码 != "" && 业务员编码 != null)
        {
            if (tmpstr != "") tmpstr += ",";
            tmpstr += "'" + 业务员编码 + "'";
        }
    }

    if (tmpstr != "")
    {
        sql += @" AND t.agntcde IN (" + tmpstr + @")";  // ✅ 直接使用 ord_ct 表字段
    }
}
```

---

## 六、验证步骤

### 步骤 1: 执行 SQL 测试脚本

在**老厂新系统数据库 (36.138.132.30)** 上执行：

```bash
sqlplus read/ejsh.read@36.138.132.30:1521/dbms @验证 SQL_利润统计.sql
```

重点关注：
- **测试 7**: `user_cde` 关联是否有数据
- **测试 8**: `empcde` 关联是否有数据
- **测试 9**: `mobile` 关联是否有数据（应该为空，验证当前代码错误）
- **测试 10**: 完整查询是否返回数据

### 步骤 2: 根据测试结果选择修复方案

- 如果**测试 7**有数据 → 使用**方案 A**（`user_cde`）
- 如果**测试 8**有数据 → 使用**方案 B**（`empcde`）
- 如果**测试 14**有数据 → 使用**方案 C**（`hr_base`）

### 步骤 3: 修改代码并编译

修改 `窗体_利润统计.cs` 中的 SQL，重新编译程序。

### 步骤 4: 测试验证

1. 打开"利润统计"窗体
2. 不勾选任何部门，直接查询（应该返回所有数据）
3. 勾选"销售 1 部"，查询（应该只返回该部门数据）
4. 勾选特定业务员，查询（应该只返回该业务员数据）

---

## 七、预期结果

### 正常情况

- **2026-03-04** 的订单数据应该能正常查询
- 业务员姓名应该正确显示（不是编码）
- 部门名称应该正确显示
- 部门筛选功能应该正常工作
- 业务员筛选功能应该正常工作

### 异常情况处理

如果查询仍然没有数据，可能原因：

1. **日期范围问题**: 2026-03-04 可能没有订单数据
   - 解决：尝试更大的日期范围（如 2026-03-01 到 2026-03-05）

2. **数据库连接问题**: 某些数据库无法连接
   - 解决：检查数据库连接字符串和网络

3. **表名前缀问题**: 某些数据库使用不同的 schema
   - 解决：检查表是否需要 `ferp.` 前缀

4. **字段名差异**: 不同子公司的表结构可能不同
   - 解决：在各子公司数据库上分别执行测试脚本

---

## 八、后续优化建议

### 8.1 统一新旧系统查询逻辑

为旧系统（临海）和新系统（老厂、新厂、温森）分别编写查询：

```csharp
if (db.ServerType == "新系统") {
    // 使用 pb_dept_member 表
    sql = 新系统 SQL;
} else {
    // 使用 pb_emps 表
    sql = 旧系统 SQL;
}
```

### 8.2 添加错误日志

在查询失败时记录详细错误信息：

```csharp
catch (Exception ex)
{
    System.Diagnostics.Debug.WriteLine($"查询 {db.FactoryName} 失败：{ex.Message}");
    System.Diagnostics.Debug.WriteLine($"SQL: {sql}");
}
```

### 8.3 添加数据验证

在查询前验证表结构和字段：

```csharp
// 检查 pb_dept_member 表是否存在
var tableExists = helper.ExecuteScalar("SELECT COUNT(*) FROM all_tables WHERE table_name = 'PB_DEPT_MEMBER'");
if (tableExists == 0) {
    // 使用备用方案
}
```

---

## 九、总结

### 问题根源

**核心问题**: `窗体_利润统计.cs` 中使用了错误的字段名 `e.mobile`，而 `pb_dept_member` 表中没有这个字段。

**次要问题**: 
- 业务员筛选条件引用了未定义的 `h` 表
- 新旧系统数据结构差异未正确处理

### 修复优先级

1. **高优先级**: 修复 JOIN 字段名（`e.mobile` → `e.user_cde` 或 `e.empcde`）
2. **高优先级**: 修复业务员筛选条件（`h.mobile` → `t.agntcde`）
3. **中优先级**: 为旧系统添加单独的查询逻辑
4. **低优先级**: 添加错误日志和数据验证

### 下一步行动

1. ✅ 已创建 SQL 测试脚本
2. ⏳ **请用户在老厂新系统数据库上执行测试脚本**
3. ⏳ 根据测试结果确定正确的字段名
4. ⏳ 修改代码并编译
5. ⏳ 测试验证功能

---

**报告生成时间**: 2026-03-05 17:45  
**SQL 脚本**: `/home/admin/.openclaw/workspace/YiJie-Query-Tool/验证 SQL_利润统计.sql`  
**修复方案**: `/home/admin/.openclaw/workspace/YiJie-Query-Tool/利润统计查询修复方案.md`
